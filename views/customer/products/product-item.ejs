<article class="product-item">
    <img src="<%= product.imageUrl  %>" alt="<%= product.title  %>">
    <div class="product-item-content">
        <h2>
            <%= product.title %>
        </h2>
        <div class="product-item-actions">
            <a class="btn btn-alt" href="/products/<%=product._id  %>">View Details</a>
        </div>
    </div>

</article>